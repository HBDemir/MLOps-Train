apiVersion: v1
kind: Service
metadata:
  name: eps-api-service
spec:
  type: NodePort
  selector:
    app: eps-api
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080 # Tarayıcıdan bu port ile erişeceğiz